GradeFlow is a privacy-first, hybrid local-cloud pipeline for automated grading of handwritten exam papers. A vision-language model running on-premises transcribes scanned answer sheets; an anonymisation layer strips student identity before any data leaves the server; a cloud LLM grades the anonymous transcriptions against a professor-defined rubric using a dual-run consistency check; and a local reconciliation step re-attaches grades to student identities.
